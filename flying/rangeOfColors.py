import numpy as np

# Range of colors in hsv
lowerBlue, upperBlue = np.array([100, 0, 0]), np.array([140, 255, 255])
lowerRed, upperRed = np.array([0, 50, 50]), np.array([4, 255, 255])
lowerBlack, upperBlack = np.array([0, 0, 0]), np.array([180, 255, 30])
lowBrown, upperBrown = np.array([10, 0, 0]), np.array([20, 255, 255])
lowerPink, upperPink = np.array([294, 0, 0]), np.array([20, 255, 255])
