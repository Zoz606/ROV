import cv2 as cv
import numpy as np
from main import *
# determine the coordenates of a point by mouse

'''
def click_event(event, x, y, flag, param):
    point = []
    # checking for left mouse clicks
    if event == cv.EVENT_LBUTTONDOWN:
        point.extend([x, y])
        # displaying the coordinates on the image window
        #cv.putText(edges, f'({x} , {y})', (x, y), cv.FONT_HERSHEY_SIMPLEX, 1, (255, 0, 0), 2)
    return point
'''


